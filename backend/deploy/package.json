{
  "name": "@cast/deploy",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "./deploy.sh",
    "deploy:stag": "sam deploy --config-env cast-stag"
  },
  "dependencies": {
    "@cast/runtime": "workspace:*",
    "@cast/server": "workspace:*",
    "@cast/storage": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
